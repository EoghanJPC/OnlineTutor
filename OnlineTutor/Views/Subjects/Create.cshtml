@model OnlineTutor.Models.Subject

@{
    ViewData["Title"] = "Create Subject";
}

<div class="container py-5">
    <div class="row justify-content-center">
        <div class="col-md-6 col-lg-5">
            
            <a asp-action="Index" class="btn btn-link text-decoration-none p-0 mb-4 text-secondary">
                <i class="bi bi-arrow-left"></i> &larr; Back to List
            </a>

            <div class="card shadow-sm border-0 bg-body-tertiary">
                <div class="card-body p-4 p-md-5">
                    <h2 class="h4 fw-bold mb-1 text-body">Create New Subject</h2>
                    <p class="text-muted small mb-4">Add a new subject to the tutoring catalog.</p>

                    <form asp-action="Create">
                        
                        <div asp-validation-summary="All" class="text-danger small mb-3 fw-semibold"></div>

                        <div class="form-floating mb-3">
                            <input asp-for="SubjectName" class="form-control" placeholder="Subject Name" />
                            <label asp-for="SubjectName" class="text-muted"></label>
                            <span asp-validation-for="SubjectName" class="text-danger small"></span>
                        </div>

                        <div class="form-floating mb-4">
                            <input asp-for="SubjectDesc" class="form-control" placeholder="Subject Description" />
                            <label asp-for="SubjectDesc" class="text-muted"></label>
                            <span asp-validation-for="SubjectDesc" class="text-danger small"></span>
                        </div>

                        <div class="d-grid">
                            <button type="submit" class="btn btn-primary py-2.5 fw-semibold">
                                Create Subject
                            </button>
                        </div>
                    </form>
                </div>
            </div>

        </div>
    </div>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}